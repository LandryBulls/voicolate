[pytest]
testpaths = tests
# dash registers a pytest plugin that imports dash at startup; in the fusion env
# that import fails (comm.create_comm) before a single test is collected.
addopts = -p no:dash
